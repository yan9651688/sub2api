# Preserve release inputs and checksummed patches byte for byte.
* -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
# Unified diff context lines intentionally contain leading spaces before tabs.
*.patch -whitespace
